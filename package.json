{
  "name": "resume-analyzer-and-generator",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm --prefix backend install",
    "postinstall": "npm --prefix backend install",
    "start": "npm --prefix backend start"
  }
}
